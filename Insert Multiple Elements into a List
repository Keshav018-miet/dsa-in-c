#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);
    int arr[110];
    for(int i = 0; i < N; i++) {
        scanf("%d", &arr[i]);
    }
    int K;
    scanf("%d", &K);
    for(int k = 0; k < K; k++) {
        int X, P;
        scanf("%d %d", &X, &P);
        for(int i = N; i > P; i--) {
            arr[i] = arr[i-1];
        }
        arr[P] = X;
        N++;
    }
    for(int i = 0; i < N; i++) {
        printf("%d", arr[i]);
        if(i < N-1) printf(" ");
    }
    printf("\n");
    
    return 0;
}
