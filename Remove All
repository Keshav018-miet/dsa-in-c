#include <stdio.h>

int main() 
{
    int n, val;
    scanf("%d", &n);    
    int arr[105];
    for(int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }
    scanf("%d", &val);
    int k = 0;
    for(int i = 0; i < n; i++) 
    {
        if(arr[i] != val) 
        {
            arr[k] = arr[i];
            k++;
        }
    }
    for(int i = 0; i < k; i++) 
    {
        if(i > 0) printf(" ");
        printf("%d", arr[i]);
    }
    printf("\n");
    
    return 0;
}
