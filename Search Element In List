#include <stdio.h>

int main() 
{
    int n, key;
    scanf("%d", &n);    
    int a[1005];
    for(int i = 0; i < n; i++) {
        scanf("%d", &a[i]);
    }   
    scanf("%d", &key);   
    for(int i = 0; i < n; i++) 
    {
        if(a[i] == key) 
        {
            printf("Found\n");
            return 0;
        }
    }    
    printf("Not Found\n");
    return 0;
}
