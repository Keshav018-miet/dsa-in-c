#include<stdio.h>
int main() {
    int n, x;
    scanf("%d", &n);
    int arr[101];
    for(int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }
    scanf("%d", &x);
    for(int i = n-1; i >= 0; i--) {
        arr[i+1] = arr[i];
    }
    arr[0] = x;
    for(int i = 0; i <= n; i++) {
        printf("%d", arr[i]);
        if(i < n) printf(" ");
    }
    printf("\n");
    
    return 0;
}
